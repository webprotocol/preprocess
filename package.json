{
  "name": "preprocess",
  "version": "1.0.0",
  "description": "",
  "main": "http://localhost:9000",
  "scripts": {
    "start": "fast-live-reload html -ep \"tsc -w --rootDir src/ts --outDir html/js\" -ep \"node-sass -w -r -o html/css src/scss\" -ep \"nw\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "nw": "nw"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^6.0.45"
  },
  "devDependencies": {
    "fast-live-reload": "^2.7.0",
    "node-sass": "^3.10.1",
    "nw": "^0.17.6",
    "typescript": "^2.0.3"
  },
  "node-remote": "http://*/*",
  "window": {
    "width": 800,
    "height": 800
  }
}
